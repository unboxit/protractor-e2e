box: nodesource/trusty
build:
  steps:
    - script:
        name: setup
        code: sudo npm install -g protractor@3.1.1 phantomjs-prebuilt
    - script:
        name: install
        code: npm install
    - script:
        name: postinstall
        code: npm run postinstall
    - script:
        name: test
        code: npm test

