box: nodesource/trusty
build:
  steps:
    - script:
        name: dependencies
        code: sudo npm install -g protractor@3.1.1 phantomjs
    - npm-install
    - script:
        name: postinstall
        code: npm run postinstall
    - npm-test

